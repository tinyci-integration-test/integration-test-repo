---
mountpoint: "/tmp/repo"
workdir: "/"
default_image: "debian:latest"
default_timeout: 5s
runs:
  sleep:
    command: [ "sleep", "10" ]
  sleep2:
    command: [ "sleep", "7" ]
  sleep-win:
    command: [ "sleep", "2" ]
