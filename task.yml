---
mountpoint: "/"
default_image: "_/debian:latest"
runs:
  sleep:
    command: [ "sleep", "1" ]
  exit0:
    command: [ "exit", "0" ]
  exit1:
    command: [ "exit", "1" ]
  catlog: 
    command: [ "cat", "/proc/cpuinfo" ]
